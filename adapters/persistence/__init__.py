"""Persistence — SQL layer.

This is the only directory in the Python codebase allowed to import `sqlalchemy`.
For now contains an in-memory implementation used during scaffolding; the real
Postgres-backed repositories arrive in the persistence sprint.
"""
