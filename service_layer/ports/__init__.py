"""Ports — Protocol definitions that adapters implement.

A use case depends only on these Protocols. Concrete adapter classes live in
`adapters/` and are wired in `entrypoints/composition_root.py`.
"""
